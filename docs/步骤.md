# 宝可梦知识图谱 — 开发步骤指南

## 开发顺序总览

| 步骤 | 模块 | 预计时间 | 验证标准 |
|------|------|----------|----------|
| 1 | 环境搭建 | 0.5 天 | Neo4j 能连接，Python 依赖装好 |
| 2 | 数据导入管线 | 1-2 天 | 数据库有数据，Cypher 能查询 |
| 3 | 后端基础框架 | 1 天 | FastAPI 启动，/api/health 能通 |
| 4 | LLM 集成 | 0.5 天 | 能调用通义千问 API |
| 5 | Cypher 生成 + 问答接口 | 2 天 | POST /api/chat 返回答案 |
| 6 | 前端开发 | 2 天 | 聊天界面能问能答 |
| 7 | 完善和测试 | 1-2 天 | 10 个测试用例通过 8 个 |

**总耗时（参考）**：约 7-9 天（3 人并行可压缩到 4-5 天）

---

## 第 1 步：环境搭建（0.5 天）

### 1.1 Neo4j 配置

项目使用 Neo4j Aura 云数据库，配置如下：

```python
NEO4J_URI = "neo4j+s://98695523.databases.neo4j.io"
NEO4J_USER = "98695523"
NEO4J_PASSWORD = "不告诉你"  # 实际使用时填入真实密码
```

**注意**：这是云端数据库（`neo4j+s://` 协议），不是本地 Neo4j Desktop。

### 1.2 安装 Python 依赖

```bash
# 创建项目目录
mkdir pokemon-kg && cd pokemon-kg

# 创建虚拟环境（可选）
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 创建 requirements.txt
cat > requirements.txt << EOF
fastapi==0.115.*
uvicorn==0.34.*
neo4j==5.*
openai>=1.0.*
httpx==0.28.*
pydantic==2.*
python-dotenv==1.0.*
EOF

# 安装
pip install -r requirements.txt
```

### 1.3 验证 Neo4j 连接

```bash
python -c "
from neo4j import GraphDatabase
driver = GraphDatabase.driver('neo4j+s://98695523.databases.neo4j.io', auth=('98695523', '你的密码'))
driver.verify_connectivity()
print('✓ Neo4j 连接成功')
driver.close()
"
```

### 1.4 验证清单

- [ ] `.env` 文件已配置 `NEO4J_URI`、`NEO4J_USER`、`NEO4J_PASSWORD`
- [ ] Python 能导入 `neo4j`、`fastapi`、`openai` 模块
- [ ] 能成功连接云端数据库（执行上面验证脚本）
- [ ] Neo4j Browser 能打开（https://console.neo4j.io/）

---

## 第 2 步：数据导入管线（1-2 天）⭐ **最优先**

### 2.1 创建目录结构

```bash
mkdir -p backend/data/{raw,processed,cypher}
mkdir -p data_pipeline
```

### 2.2 抓取数据（fetch_pokeapi.py）

```python
# data_pipeline/fetch_pokeapi.py
import requests
import time
import json
from pathlib import Path

def fetch_pokemon(limit=10):
    """抓取前 N 只 Pokemon（先用 10 只测试）"""
    raw_dir = Path("backend/data/raw")
    raw_dir.mkdir(parents=True, exist_ok=True)
    
    for i in range(1, limit + 1):
        path = raw_dir / f"pokemon_{i}.json"
        if path.exists():
            print(f"跳过已存在：pokemon_{i}")
            continue
        
        resp = requests.get(f"https://pokeapi.co/api/v2/pokemon/{i}")
        if resp.status_code == 200:
            with open(path, "w") as f:
                json.dump(resp.json(), f)
            print(f"✓ 抓取：pokemon_{i}")
        else:
            print(f"✗ 失败：pokemon_{i}")
        time.sleep(0.5)  # 限速

def fetch_types():
    """抓取 18 种属性"""
    raw_dir = Path("backend/data/raw")
    for i in range(1, 19):
        resp = requests.get(f"https://pokeapi.co/api/v2/type/{i}")
        if resp.status_code == 200:
            with open(raw_dir / f"type_{i}.json", "w") as f:
                json.dump(resp.json(), f)
    print("✓ Type 抓取完成")

if __name__ == "__main__":
    fetch_pokemon(10)  # 先抓 10 只测试
    fetch_types()
```

**运行**：
```bash
cd data_pipeline
python fetch_pokeapi.py
```

### 2.3 转换数据（transform.py）

```python
# data_pipeline/transform.py
import json
from pathlib import Path

def transform_pokemon():
    """将原始 JSON 转换为 Neo4j 导入格式"""
    raw_dir = Path("backend/data/raw")
    processed_dir = Path("backend/data/processed")
    processed_dir.mkdir(parents=True, exist_ok=True)
    
    pokemon_nodes = []
    relations = []
    
    for f in raw_dir.glob("pokemon_*.json"):
        with open(f) as fp:
            data = json.load(fp)
        
        # 提取 Pokemon 节点
        pokemon_nodes.append({
            "id": data["id"],
            "name": data["name"],
            "height": data["height"],
            "weight": data["weight"],
            "base_experience": data["base_experience"]
        })
        
        # 提取 HAS_TYPE 关系
        for slot, type_info in enumerate(data["types"], 1):
            relations.append({
                "pokemon_id": data["id"],
                "type_name": type_info["type"]["name"],
                "slot": slot
            })
    
    # 保存
    with open(processed_dir / "pokemon_nodes.json", "w") as f:
        json.dump(pokemon_nodes, f, ensure_ascii=False, indent=2)
    
    with open(processed_dir / "has_type_relations.json", "w") as f:
        json.dump(relations, f, ensure_ascii=False, indent=2)
    
    print(f"✓ 转换完成：{len(pokemon_nodes)} 只 Pokemon, {len(relations)} 条关系")

if __name__ == "__main__":
    transform_pokemon()
```

**运行**：
```bash
python transform.py
```

### 2.4 Cypher 导入脚本

```cypher
-- backend/data/cypher/import_types.cypher
-- 创建 18 个 Type 节点
UNWIND $batch AS row
MERGE (t:Type {id: row.id})
SET t.name = row.name
```

```cypher
-- backend/data/cypher/init_constraints.cypher
-- 创建唯一约束
CREATE CONSTRAINT pokemon_id IF NOT EXISTS
FOR (p:Pokemon) REQUIRE p.id IS UNIQUE;

CREATE CONSTRAINT type_id IF NOT EXISTS
FOR (t:Type) REQUIRE t.id IS UNIQUE;
```

```cypher
-- backend/data/cypher/import_pokemon.cypher
-- 创建 Pokemon 节点
UNWIND $batch AS row
MERGE (p:Pokemon {id: row.id})
SET p.name = row.name,
    p.height = row.height,
    p.weight = row.weight,
    p.base_experience = row.base_experience
```

```cypher
-- backend/data/cypher/import_has_type.cypher
-- 创建 HAS_TYPE 关系
UNWIND $batch AS row
MATCH (p:Pokemon {id: row.pokemon_id})
MATCH (t:Type {name: row.type_name})
MERGE (p)-[r:HAS_TYPE]->(t)
SET r.slot = row.slot
```

### 2.5 批量导入脚本

```python
# data_pipeline/load_to_neo4j.py
import json
import os
from pathlib import Path
from neo4j import GraphDatabase
from dotenv import load_dotenv

load_dotenv()

# 从环境变量读取配置
URI = os.getenv("NEO4J_URI", "neo4j+s://98695523.databases.neo4j.io")
AUTH = (
    os.getenv("NEO4J_USER", "98695523"),
    os.getenv("NEO4J_PASSWORD")
)

def load_cypher_file(filename):
    """执行 Cypher 文件"""
    with open(Path("backend/data/cypher") / filename) as f:
        cypher = f.read()
    
    with GraphDatabase.driver(URI, auth=AUTH) as driver:
        with driver.session() as session:
            session.run(cypher)
    print(f"✓ 执行：{filename}")

def load_with_data(filename, data_file):
    """执行带数据的 Cypher（批量导入）"""
    with open(Path("backend/data/processed") / data_file) as f:
        data = json.load(f)
    
    with open(Path("backend/data/cypher") / filename) as f:
        cypher = f.read()
    
    with GraphDatabase.driver(URI, auth=AUTH) as driver:
        with driver.session() as session:
            # 分批导入，每批 50 条
            for i in range(0, len(data), 50):
                batch = data[i:i+50]
                session.run(cypher, batch=batch)
    print(f"✓ 导入：{filename} ({len(data)} 条)")

if __name__ == "__main__":
    # 1. 约束
    load_cypher_file("init_constraints.cypher")
    
    # 2. Type 节点（手动构造）
    types = [{"id": i, "name": ""} for i in range(1, 19)]  # 需要从 type JSON 提取
    # 简化：直接读取 type JSON 转换
    
    # 3. Pokemon 节点
    load_with_data("import_pokemon.cypher", "pokemon_nodes.json")
    
    # 4. HAS_TYPE 关系
    load_with_data("import_has_type.cypher", "has_type_relations.json")
    
    print("✓ 导入完成")
```

### 2.6 验证

打开 Neo4j Browser（https://console.neo4j.io/），执行：
```cypher
MATCH (p:Pokemon)-[:HAS_TYPE]->(t:Type) 
RETURN p.name, t.name 
LIMIT 5
```

预期看到类似：
```
| p.name    | t.name    |
|-----------|-----------|
| bulbasaur | grass     |
| bulbasaur | poison    |
| ivysaur   | grass     |
| ...       | ...       |
```

---

## 第 3 步：后端基础框架（1 天）

### 3.1 配置文件（config.py）

```python
# backend/config.py
import os
from dotenv import load_dotenv

load_dotenv()

# Neo4j Aura 云数据库配置
NEO4J_URI = os.getenv("NEO4J_URI", "neo4j+s://98695523.databases.neo4j.io")
NEO4J_USER = os.getenv("NEO4J_USER", "98695523")
NEO4J_PASSWORD = os.getenv("NEO4J_PASSWORD")  # 必须从环境变量读取

DASHSCOPE_API_KEY = os.getenv("DASHSCOPE_API_KEY", "")
DASHSCOPE_BASE_URL = "https://dashscope.aliyuncs.com/compatible-mode/v1"
LLM_MODEL = os.getenv("LLM_MODEL", "qwen-plus")
```

### 3.2 FastAPI 入口（main.py）

```python
# backend/main.py
from fastapi import FastAPI
from routers import query, chat

app = FastAPI(title="Pokemon KG API")

app.include_router(query.router, prefix="/api", tags=["query"])
app.include_router(chat.router, prefix="/api", tags=["chat"])

@app.get("/api/health")
def health():
    return {"status": "ok", "service": "pokemon-kg"}

if __name__ == "__main__":
    import uvicorn
    uvicorn.run(app, host="0.0.0.0", port=8000)
```

### 3.3 Neo4j 服务（neo4j_service.py）

```python
# backend/services/neo4j_service.py
from neo4j import GraphDatabase
from config import NEO4J_URI, NEO4J_USER, NEO4J_PASSWORD

class Neo4jService:
    def __init__(self):
        self.driver = GraphDatabase.driver(NEO4J_URI, auth=(NEO4J_USER, NEO4J_PASSWORD))
    
    def execute(self, cypher: str, params: dict = None):
        with self.driver.session() as session:
            result = session.run(cypher, params or {})
            return [record.data() for record in result]
    
    def close(self):
        self.driver.close()

neo4j = Neo4jService()
```

### 3.4 查询路由（query.py）

```python
# backend/routers/query.py
from fastapi import APIRouter
from pydantic import BaseModel
from services.neo4j_service import neo4j

router = APIRouter()

class QueryRequest(BaseModel):
    cypher: str
    parameters: dict = {}

@router.post("/query")
def query_database(req: QueryRequest):
    """直接执行 Cypher 查询（调试用）"""
    data = neo4j.execute(req.cypher, req.parameters)
    return {"data": data}
```

### 3.5 验证

```bash
# 启动后端
cd backend
uvicorn main:app --reload

# 测试健康检查
curl http://localhost:8000/api/health

# 测试查询
curl -X POST http://localhost:8000/api/query \
  -H "Content-Type: application/json" \
  -d '{"cypher": "MATCH (p:Pokemon) RETURN p.name LIMIT 3"}'
```

---

## 第 4 步：LLM 集成（0.5 天）

### 4.1 LLM 服务（llm_service.py）

```python
# backend/services/llm_service.py
from openai import OpenAI
from config import DASHSCOPE_API_KEY, DASHSCOPE_BASE_URL, LLM_MODEL

client = OpenAI(
    api_key=DASHSCOPE_API_KEY,
    base_url=DASHSCOPE_BASE_URL
)

def call_llm(prompt: str, temperature: float = 0.1) -> str:
    """调用通义千问 API"""
    response = client.chat.completions.create(
        model=LLM_MODEL,
        messages=[{"role": "user", "content": prompt}],
        temperature=temperature
    )
    return response.choices[0].message.content

if __name__ == "__main__":
    # 测试
    result = call_llm("1+1 等于几？请用中文回答")
    print(f"LLM 返回：{result}")
```

### 4.2 验证

```bash
cd backend
python services/llm_service.py
```

看到类似 `LLM 返回：1+1 等于 2` 即成功。

---

## 第 5 步：Cypher 生成 + 问答接口（2 天）⭐ **核心功能**

### 5.1 Schema 服务（schema_provider.py）

```python
# backend/services/schema_provider.py
from pathlib import Path

SCHEMA_PATH = Path(__file__).resolve().parents[2] / "docs" / "schema.md"

def get_schema() -> str:
    return SCHEMA_PATH.read_text(encoding="utf-8").strip()
```

### 5.2 Cypher 生成器（cypher_generator.py）

```python
# backend/services/cypher_generator.py
from services.llm_service import call_llm
from services.schema_provider import get_schema

CYPHER_PROMPT = """
你是一个宝可梦知识图谱的 Cypher 查询专家。根据用户的问题，生成对应的 Cypher 查询语句。

## 图谱 Schema
{schema}

## 示例
用户问："皮卡丘有什么属性？"
返回：MATCH (p:Pokemon {{name: "pikachu"}})-[:HAS_TYPE]->(t:Type) RETURN t.name

用户问："火属性克制哪些属性？"
返回：MATCH (f:Type {{name: "fire"}})-[:DAMAGE_TO {{multiplier: 2.0}}]->(t:Type) RETURN t.name

## 用户问题
{question}

请只返回 Cypher 查询语句，不要包含其他内容。
""".strip()

def generate_cypher(question: str) -> str:
    """根据问题生成 Cypher"""
    prompt = (
        CYPHER_PROMPT
        .replace("{schema}", get_schema())
        .replace("{question}", question)
    )
    cypher = call_llm(prompt, temperature=0.1)
    # 清理可能的markdown标记
    cypher = cypher.strip().replace("```cypher", "").replace("```", "").strip()
    return cypher

if __name__ == "__main__":
    q = "皮卡丘有什么属性？"
    print(f"问题：{q}")
    print(f"生成 Cypher: {generate_cypher(q)}")
```

### 5.3 答案生成器（answer_generator.py）

```python
# backend/services/answer_generator.py
from services.llm_service import call_llm
import json

ANSWER_PROMPT = """
你是一个宝可梦知识图谱问答助手。根据用户的提问和图数据库查询结果，生成自然语言回答。

## 用户提问
{question}
## 查询结果
{results}

请用简洁友好的中文回答。如果查询结果为空，请告知未找到相关信息。
""".strip()

def generate_answer(question: str, results: list) -> str:
    """根据查询结果生成自然语言回答"""
    results_text = json.dumps(results, ensure_ascii=False, indent=2) if results else "[]（无结果）"
    prompt = ANSWER_PROMPT.format(question=question, results=results_text)
    return call_llm(prompt, temperature=0.3)
```

### 5.4 问答路由（chat.py）

```python
# backend/routers/chat.py
from fastapi import APIRouter
from pydantic import BaseModel
from services.cypher_generator import generate_cypher
from services.answer_generator import generate_answer
from services.neo4j_service import neo4j

router = APIRouter()

class ChatRequest(BaseModel):
    question: str

class ChatResponse(BaseModel):
    answer: str
    cypher: str
    data: list

@router.post("/chat")
def chat(req: ChatRequest):
    """主问答接口：问题 → Cypher → 查询 → 回答"""
    # 1. 生成 Cypher
    cypher = generate_cypher(req.question)
    
    # 2. 执行查询
    try:
        data = neo4j.execute(cypher)
    except Exception as e:
        return ChatResponse(
            answer=f"查询执行失败：{e}",
            cypher=cypher,
            data=[]
        )
    
    # 3. 生成回答
    answer = generate_answer(req.question, data)
    
    return ChatResponse(answer=answer, cypher=cypher, data=data)
```

### 5.5 验证

```bash
cd backend
uvicorn main:app --reload

# 测试问答
curl -X POST http://localhost:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"question": "皮卡丘有什么属性？"}'
```

预期返回：
```json
{
  "answer": "皮卡丘是电属性宝可梦",
  "cypher": "MATCH (p:Pokemon {name: \"pikachu\"})-[:HAS_TYPE]->(t:Type) RETURN t.name",
  "data": [{"t.name": "electric"}]
}
```

---

## 第 6 步：前端开发（2 天）

### 6.1 创建 Vue 项目

```bash
cd ..  # 回到项目根目录
npm create vite@latest frontend -- --template vue
cd frontend
npm install
npm install axios
```

### 6.2 API 封装（src/api/index.js）

```javascript
// frontend/src/api/index.js
import axios from 'axios'

const api = axios.create({
  baseURL: 'http://localhost:8000/api',
  timeout: 30000
})

export function chat(question) {
  return api.post('/chat', { question })
}

export function query(cypher) {
  return api.post('/query', { cypher })
}
```

### 6.3 主页面（src/App.vue）

```vue
<!-- frontend/src/App.vue -->
<template>
  <div class="chat-container">
    <h1>宝可梦知识图谱问答</h1>
    <div class="messages">
      <div v-for="(msg, i) in messages" :key="i" :class="['message', msg.role]">
        <strong>{{ msg.role === 'user' ? '你' : 'AI' }}:</strong>
        <p>{{ msg.content }}</p>
      </div>
      <div v-if="loading" class="message ai loading">AI 思考中...</div>
    </div>
    <div class="input-area">
      <input v-model="input" @keyup.enter="send" placeholder="输入问题..." />
      <button @click="send" :disabled="loading">发送</button>
    </div>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import { chat } from './api'

const input = ref('')
const messages = ref([])
const loading = ref(false)

async function send() {
  if (!input.value.trim() || loading.value) return
  const q = input.value.trim()
  messages.value.push({ role: 'user', content: q })
  input.value = ''
  loading.value = true
  
  try {
    const res = await chat(q)
    messages.value.push({ role: 'ai', content: res.data.answer })
  } catch (e) {
    messages.value.push({ role: 'ai', content: '请求失败：' + e.message })
  }
  loading.value = false
}
</script>

<style>
.chat-container { max-width: 800px; margin: 0 auto; padding: 20px; }
.messages { border: 1px solid #ddd; padding: 20px; min-height: 400px; }
.message { margin: 10px 0; padding: 10px; border-radius: 8px; }
.message.user { background: #e3f2fd; }
.message.ai { background: #f5f5f5; }
.input-area { margin-top: 20px; display: flex; gap: 10px; }
.input-area input { flex: 1; padding: 10px; }
.input-area button { padding: 10px 20px; }
</style>
```

### 6.4 启动前端

```bash
cd frontend
npm run dev
# 打开 http://localhost:5173
```

---

## 第 7 步：完善和测试（1-2 天）

### 7.1 补充数据

将抓取数据从 10 只扩展到 151 只：
```bash
cd data_pipeline
python fetch_pokeapi.py  # 修改 limit=151
python transform.py
python load_to_neo4j.py
```

### 7.2 测试用例

创建 `tests/test_cases.md`，执行以下测试：

| 问题 | 预期 |
|------|------|
| "皮卡丘是什么属性？" | 电属性 |
| "小火龙有什么特性？" | 列出特性 |
| "火属性克制什么？" | 草、冰、虫、钢 |

### 7.3 异常处理

在 `chat.py` 中增加：
- Cypher 语法错误捕获
- LLM 返回空的处理
- Neo4j 连接超时处理

---

## 三人分工建议（参考）

| 时间 | 成员 A（后端 + 数据） | 成员 B（前端） | 成员 C（LLM+ 本体） |
|------|---------------------|---------------|-------------------|
| 第 1 天 | 步骤 1-2：环境 + 数据导入 | 步骤 1：环境 + Vue 初始化 | 步骤 4：LLM 调用测试 |
| 第 2 天 | 步骤 3：后端框架 | 静态页面 | 步骤 5：Cypher 生成 |
| 第 3 天 | 步骤 5：问答接口 | 等 API | 步骤 5：答案生成 |
| 第 4 天 | 联调 | 步骤 6：前端对接 | 测试 + Prompt 调优 |
| 第 5 天 | 步骤 7：完善 | 步骤 7：完善 | 步骤 7：完善 |

---


---

## 附录：.env 配置示例

```bash
# Neo4j Aura 云数据库
NEO4J_URI=neo4j+s://98695523.databases.neo4j.io
NEO4J_USER=98695523
NEO4J_PASSWORD=你的真实密码

# 通义千问 API
DASHSCOPE_API_KEY=sk-your_api_key_here
LLM_MODEL=qwen-plus
```

